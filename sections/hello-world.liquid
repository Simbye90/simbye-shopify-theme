<!-- sections/hero-simbye.liquid -->
<!-- SIMBYE HERO - OPTIMIZED FOR SEO & MOBILE -->

<style>
/* Global Reset */
.hero-section-wrapper {
  width: 100%;
  margin: 0;
  padding: 0;
  overflow: hidden;
}

/* Hero Section - FULL WIDTH */
.hero-enterprise {
  width: 100%;
  padding: 40px 0 60px 0; /* Kein seitliches Padding = Full Width */
  background: linear-gradient(180deg, 
    #ffffff 0%,
    #ffffff 10%,
    #f9fffd 30%,
    #e8f7f3 70%,
    #daf2eb 100%
  );
  position: relative;
}

/* Container für Content - mit Padding */
.hero-container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 20px; /* Padding nur im Container */
  width: 100%;
}

/* MOBILE FIRST */
.hero-content {
  text-align: center;
  width: 100%;
}

/* Title Mobile */
.hero-title {
  font-size: 38px;
  font-weight: 900;
  line-height: 1.05;
  margin: 0 0 24px 0;
  letter-spacing: -0.02em;
}

.hero-title-primary {
  display: block;
  color: #0a0a0a;
  margin-bottom: 6px;
}

.hero-title-gradient {
  display: block;
  background: linear-gradient(135deg, #009a61 0%, #00d97e 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Subtitle - NUR DESKTOP */
.hero-subtitle {
  display: none; /* Mobile: ausgeblendet */
}

/* Button Mobile */
.hero-cta {
  margin-bottom: 36px;
}

.btn-hero-main {
  width: 100%;
  max-width: 300px;
  padding: 18px 32px;
  font-size: 18px;
  font-weight: 700;
  color: white;
  background: var(--primary-color, #009a61);
  border: none;
  border-radius: 50px;
  cursor: pointer;
  box-shadow: 0 4px 16px rgba(0, 154, 97, 0.2);
  transition: all 0.3s ease;
  white-space: nowrap;
}

/* Stats Mobile */
.hero-stats {
  display: flex;
  justify-content: center;
  gap: 20px;
  margin-bottom: 36px;
  width: 100%;
}

.stat-item {
  text-align: center;
  flex: 1;
  max-width: 90px;
}

.stat-number {
  display: block;
  font-size: 26px;
  font-weight: 900;
  color: var(--primary-color, #009a61);
  margin-bottom: 4px;
}

.stat-label {
  display: block;
  font-size: 11px;
  font-weight: 600;
  color: #6b6b6b;
  line-height: 1.3;
}

/* Image Mobile - FULL WIDTH */
.hero-visual {
  width: 100vw;
  margin-left: calc(-50vw + 50%);
  position: relative;
}

.hero-phones {
  width: 100%;
  height: auto;
  display: block;
}

/* Product Labels - Hidden Mobile */
.product-labels {
  display: none;
}

/* Mobile Badges */
.mobile-products {
  display: flex;
  gap: 8px;
  justify-content: center;
  margin-top: -20px;
  margin-bottom: 20px;
  flex-wrap: wrap;
  padding: 0 20px;
  position: relative;
  z-index: 2;
}

.mobile-product-badge {
  padding: 10px 14px;
  background: white;
  border-radius: 20px;
  font-size: 13px;
  font-weight: 700;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  white-space: nowrap;
}

.mobile-product-badge .price {
  color: var(--primary-color, #009a61);
  font-weight: 800;
}

/* TABLET (768px+) */
@media (min-width: 768px) {
  .hero-enterprise {
    padding: 50px 0 70px 0;
  }
  
  .hero-container {
    padding: 0 30px;
  }
  
  .hero-title {
    font-size: 52px;
    margin-bottom: 20px;
  }
  
  /* Subtitle zeigen auf Tablet */
  .hero-subtitle {
    display: block;
    font-size: 18px;
    font-weight: 500;
    color: #5a5a5a;
    line-height: 1.5;
    margin: 0 0 36px 0;
  }
  
  .btn-hero-main {
    width: auto;
    padding: 20px 48px;
    max-width: none;
  }
  
  .hero-stats {
    gap: 36px;
  }
  
  .stat-number {
    font-size: 32px;
  }
  
  .stat-label {
    font-size: 13px;
  }
  
  .hero-visual {
    width: 100%;
    margin: 0 auto;
    max-width: 600px;
  }
}

/* DESKTOP (1024px+) */
@media (min-width: 1024px) {
  .hero-enterprise {
    padding: 60px 0 80px 0; /* Full width background */
  }
  
  .hero-container {
    padding: 0 40px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }
  
  /* Content Links wie Logo */
  .hero-content {
    text-align: left;
  }
  
  .hero-title {
    font-size: 64px;
    margin-bottom: 24px;
  }
  
  .hero-title-primary {
    margin-bottom: 10px;
  }
  
  .hero-subtitle {
    font-size: 20px;
    padding: 0;
    max-width: 480px;
    margin-bottom: 40px;
  }
  
  .hero-cta {
    margin-bottom: 50px;
  }
  
  .btn-hero-main:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 28px rgba(0, 154, 97, 0.3);
    background: #00824f;
  }
  
  .hero-stats {
    justify-content: flex-start;
    gap: 50px;
  }
  
  .stat-item {
    text-align: left;
  }
  
  .stat-number {
    font-size: 38px;
  }
  
  .stat-label {
    font-size: 14px;
  }
  
  /* Visual Desktop */
  .hero-visual {
    width: 100%;
    margin: 0;
    max-width: none;
    position: relative;
  }
  
  .hero-phones {
    max-width: 650px;
    width: 100%;
    filter: drop-shadow(0 20px 40px rgba(0, 0, 0, 0.1));
  }
  
  /* Product Labels Desktop */
  .product-labels {
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
  }
  
  .product-label {
    position: absolute;
    padding: 12px 20px;
    background: white;
    border-radius: 26px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
    font-weight: 700;
    font-size: 14px;
    display: flex;
    align-items: center;
    gap: 10px;
    transition: transform 0.3s ease;
  }
  
  .product-label:hover {
    transform: scale(1.05);
  }
  
  .product-label-1 {
    top: 15%;
    left: -12%;
  }
  
  .product-label-2 {
    top: 45%;
    right: -8%;
  }
  
  .product-label-3 {
    bottom: 25%;
    left: 5%;
  }
  
  .label-price {
    color: var(--primary-color, #009a61);
    font-weight: 800;
  }
  
  /* Hide mobile badges */
  .mobile-products {
    display: none;
  }
}

/* XL Desktop */
@media (min-width: 1440px) {
  .hero-title {
    font-size: 72px;
  }
  
  .hero-subtitle {
    font-size: 22px;
  }
}

/* Dark Mode */
[data-theme="dark"] .hero-enterprise {
  background: linear-gradient(180deg,
    #0a0a0a 0%,
    #0a0a0a 10%,
    #0f1210 30%,
    #0a1510 70%,
    #051008 100%
  );
}

[data-theme="dark"] .hero-title-primary {
  color: #f5f5f5;
}

[data-theme="dark"] .hero-subtitle {
  color: #b0b0b0;
}

[data-theme="dark"] .mobile-product-badge,
[data-theme="dark"] .product-label {
  background: #1a1a1a;
  color: #f0f0f0;
}

/* Animations */
.hero-content > * {
  animation: fadeInUp 0.6s ease both;
}

.hero-content > *:nth-child(2) { animation-delay: 0.1s; }
.hero-content > *:nth-child(3) { animation-delay: 0.2s; }
.hero-content > *:nth-child(4) { animation-delay: 0.3s; }

.hero-visual {
  animation: fadeIn 0.8s ease 0.4s both;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
</style>

<div class="hero-section-wrapper">
  <section class="hero-enterprise" role="banner" aria-label="{{ 'hero.section_label' | t | default: 'Hero Section' }}">
    <div class="hero-container">
      <!-- Content -->
      <div class="hero-content">
        <!-- Title -->
        <h1 class="hero-title">
          <span class="hero-title-primary">
            {{ 'hero.title_line1' | t | default: 'Günstige eSIM' }}
          </span>
          <span class="hero-title-gradient">
            {{ 'hero.title_line2' | t | default: 'Datenpakete für Reisen' }}
          </span>
        </h1>
        
        <!-- Subtitle - Nur Desktop/Tablet -->
        <p class="hero-subtitle">
          {{ 'hero.subtitle' | t | default: 'Internet in 190+ Ländern • Keine Roaming-Gebühren • In 2 Minuten aktiviert' }}
        </p>
        
        <!-- CTA Button -->
        <div class="hero-cta">
          <button 
            class="btn-hero-main" 
            onclick="window.location.href='{{ routes.root_url }}collections/free-esim'"
            type="button"
            aria-label="{{ 'hero.cta_aria' | t | default: 'Jetzt kostenlose eSIM holen' }}"
          >
            {{ 'hero.cta_text' | t | default: 'Kostenlose eSIM' }}
          </button>
        </div>
        
        <!-- Stats -->
        <div class="hero-stats" role="list">
          <div class="stat-item" role="listitem">
            <span class="stat-number">190+</span>
            <span class="stat-label">{{ 'hero.stat_countries' | t | default: 'Länder verfügbar' }}</span>
          </div>
          
          <div class="stat-item" role="listitem">
            <span class="stat-number">2 Min</span>
            <span class="stat-label">{{ 'hero.stat_activation' | t | default: 'Installation' }}</span>
          </div>
          
          <div class="stat-item" role="listitem">
            <span class="stat-number">100K+</span>
            <span class="stat-label">{{ 'hero.stat_customers' | t | default: 'Zufriedene Kunden' }}</span>
          </div>
        </div>
      </div>
      
      <!-- Visual -->
      <div class="hero-visual">
        <img 
          class="hero-phones"
          src="{{ section.settings.hero_image | default: 'https://cdn.shopify.com/s/files/1/0633/9012/2175/files/3-iphones-min-min.webp?v=1742328516' }}"
          alt="{{ 'hero.image_alt' | t | default: 'Simbye eSIM auf drei Smartphones mit Reisebildern' }}"
          width="650"
          height="520"
          loading="eager"
          fetchpriority="high"
        >
        
        <!-- Desktop Product Labels -->
        <div class="product-labels" aria-hidden="true">
          <div class="product-label product-label-1">
            <span aria-hidden="true">🇹🇷</span>
            <span>{{ 'hero.product_turkey' | t | default: 'Türkei • 5GB' }}</span>
            <span class="label-price">{{ 'hero.price_turkey' | t | default: '9€' }}</span>
          </div>
          
          <div class="product-label product-label-2">
            <span aria-hidden="true">🇺🇸</span>
            <span>{{ 'hero.product_usa' | t | default: 'USA • 10GB' }}</span>
            <span class="label-price">{{ 'hero.price_usa' | t | default: '19€' }}</span>
          </div>
          
          <div class="product-label product-label-3">
            <span aria-hidden="true">🇯🇵</span>
            <span>{{ 'hero.product_japan' | t | default: 'Japan • 3GB' }}</span>
            <span class="label-price">{{ 'hero.price_japan' | t | default: '12€' }}</span>
          </div>
        </div>
        
        <!-- Mobile Product Badges -->
        <div class="mobile-products" aria-hidden="true">
          <span class="mobile-product-badge">
            🇹🇷 {{ 'hero.mobile_turkey' | t | default: 'Türkei ab' }} <span class="price">9€</span>
          </span>
          <span class="mobile-product-badge">
            🇺🇸 {{ 'hero.mobile_usa' | t | default: 'USA ab' }} <span class="price">19€</span>
          </span>
          <span class="mobile-product-badge">
            🇯🇵 {{ 'hero.mobile_japan' | t | default: 'Japan ab' }} <span class="price">12€</span>
          </span>
        </div>
      </div>
    </div>
  </section>
</div>

<!-- Schema für SEO -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Product",
  "name": "{{ 'hero.schema_name' | t | default: 'Simbye eSIM Datenpakete' }}",
  "description": "{{ 'hero.schema_description' | t | default: 'eSIM Datenpakete für 190+ Länder. Keine Roaming-Gebühren. In 2 Minuten aktiviert.' }}",
  "brand": {
    "@type": "Brand",
    "name": "Simbye"
  },
  "offers": {
    "@type": "AggregateOffer",
    "priceCurrency": "EUR",
    "lowPrice": "0",
    "highPrice": "99"
  }
}
</script>

{% schema %}
{
  "name": "Hero Section",
  "class": "hero-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Bild (Optional)",
      "info": "Standard: 3 iPhones Bild"
    }
  ],
  "presets": [
    {
      "name": "Hero Section"
    }
  ]
}
{% endschema %}