<!-- sections/header.liquid -->
<!-- Enterprise Header 2025 - Simbye eSIM -->

<style>
/* Import Inter Font */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

/* CSS Custom Properties für Theming */
:root {
  --header-height: 70px;
  --header-mobile-height: 60px;
  --primary-color: #009a61;
  --primary-hover: #00824f;
  --text-color: #2c2c2c;
  --bg-header: #ffffff;
  --border-color: rgba(0, 0, 0, 0.08);
  --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.06);
  --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.08);
  --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-base: 250ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-slow: 350ms cubic-bezier(0.4, 0, 0.2, 1);
  --mobile-menu-width: 85%;
  --mobile-menu-bg: #fafafa;
  --font-base: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
}

/* Dark Mode Variables */
[data-theme="dark"] {
  --text-color: #e8e8e8;
  --bg-header: #1a1a1a;
  --border-color: rgba(255, 255, 255, 0.1);
  --mobile-menu-bg: #242424;
  --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.3);
}

/* Prevent CLS with fixed heights */
.enterprise-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: var(--header-height);
  background: var(--bg-header);
  border-bottom: 1px solid var(--border-color);
  z-index: 1000;
  contain: layout style paint;
  will-change: transform;
  transform: translateZ(0);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  transition: box-shadow var(--transition-base);
}

.enterprise-header.scrolled {
  box-shadow: var(--shadow-sm);
}

.header-container {
  display: grid;
  grid-template-columns: 200px 1fr auto;
  align-items: center;
  height: 100%;
  max-width: 1440px;
  margin: 0 auto;
  padding: 0 24px;
  gap: 32px;
}

/* Logo Section */
.header-logo {
  display: flex;
  align-items: center;
  height: 100%;
}

.header-logo img {
  width: auto;
  height: 32px;
  object-fit: contain;
}

/* Navigation - Desktop */
.header-nav {
  display: flex;
  align-items: center;
  gap: 8px;
  height: 100%;
}

.nav-item {
  position: relative;
  height: 100%;
  display: flex;
  align-items: center;
}

.nav-link {
  display: inline-flex;
  align-items: center;
  padding: 8px 16px;
  color: var(--text-color);
  font-size: 15px;
  font-weight: 500;
  text-decoration: none;
  border-radius: 8px;
  transition: all var(--transition-fast);
  position: relative;
  font-family: var(--font-base);
}

.nav-link:hover {
  background: rgba(0, 154, 97, 0.08);
  color: var(--primary-color);
}

.nav-link::after {
  content: '';
  position: absolute;
  bottom: -1px;
  left: 50%;
  width: 0;
  height: 2px;
  background: var(--primary-color);
  transform: translateX(-50%);
  transition: width var(--transition-fast);
}

.nav-link.active::after {
  width: calc(100% - 32px);
}

/* Actions Section */
.header-actions {
  display: flex;
  align-items: center;
  gap: 16px;
  height: 100%;
}

/* Auth Buttons */
.auth-group {
  display: flex;
  height: 40px;
  border-radius: 20px;
  overflow: hidden;
  box-shadow: 0 0 0 1px var(--border-color);
  background: var(--bg-header);
}

.auth-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 20px;
  height: 100%;
  font-size: 14px;
  font-weight: 500;
  color: white;
  background: var(--primary-color);
  text-decoration: none;
  transition: background var(--transition-fast);
  white-space: nowrap;
  font-family: var(--font-base);
}

.auth-btn:hover {
  background: var(--primary-hover);
}

.auth-btn.login {
  border-radius: 20px 0 0 20px;
  border-right: 1px solid rgba(255, 255, 255, 0.2);
}

.auth-btn.register {
  border-radius: 0 20px 20px 0;
  background: var(--text-color);
}

.auth-btn.register:hover {
  background: #4a4a4a;
}

.auth-btn.logged-in {
  border-radius: 20px;
  padding: 0 24px;
  gap: 8px;
}

/* Language & Currency Switchers */
.switcher-group {
  display: flex;
  gap: 8px;
}

.switcher {
  display: flex;
  align-items: center;
  gap: 4px;
  padding: 8px 12px;
  border-radius: 8px;
  background: transparent;
  border: 1px solid var(--border-color);
  color: var(--text-color);
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all var(--transition-fast);
  font-family: var(--font-base);
}

.switcher:hover {
  border-color: var(--primary-color);
  background: rgba(0, 154, 97, 0.05);
}

.switcher-icon {
  width: 20px;
  height: 20px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
}

/* Dark Mode Toggle */
.theme-toggle {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: transparent;
  border: 1px solid var(--border-color);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.theme-toggle:hover {
  background: rgba(0, 154, 97, 0.05);
  border-color: var(--primary-color);
}

.theme-toggle svg {
  width: 20px;
  height: 20px;
  transition: transform var(--transition-base);
}

.theme-toggle:active svg {
  transform: rotate(30deg);
}

/* Mobile Menu Toggle - Modern Dot Grid Design */
.mobile-toggle {
  display: none;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
  border-radius: 12px;
  background: linear-gradient(135deg, var(--primary-color), var(--primary-hover));
  cursor: pointer;
  z-index: 1001;
  position: relative;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0, 154, 97, 0.2);
  transition: all var(--transition-base);
}

.mobile-toggle:hover {
  transform: scale(1.05);
  box-shadow: 0 4px 12px rgba(0, 154, 97, 0.3);
}

.mobile-toggle:active {
  transform: scale(0.95);
}

.mobile-toggle .menu-icon {
  display: grid;
  grid-template-columns: repeat(3, 4px);
  grid-template-rows: repeat(3, 4px);
  gap: 4px;
  transition: all var(--transition-base);
}

.mobile-toggle .menu-icon span {
  display: block;
  width: 4px;
  height: 4px;
  background: white;
  border-radius: 50%;
  transition: all var(--transition-base);
}

/* Animated X pattern when active */
.mobile-toggle.active .menu-icon {
  transform: rotate(45deg);
}

.mobile-toggle.active .menu-icon span:nth-child(1),
.mobile-toggle.active .menu-icon span:nth-child(5),
.mobile-toggle.active .menu-icon span:nth-child(9) {
  transform: scale(1.5);
  background: rgba(255, 255, 255, 1);
}

.mobile-toggle.active .menu-icon span:nth-child(3),
.mobile-toggle.active .menu-icon span:nth-child(7) {
  transform: scale(1.5);
  background: rgba(255, 255, 255, 1);
}

.mobile-toggle.active .menu-icon span:nth-child(2),
.mobile-toggle.active .menu-icon span:nth-child(4),
.mobile-toggle.active .menu-icon span:nth-child(6),
.mobile-toggle.active .menu-icon span:nth-child(8) {
  transform: scale(0);
  opacity: 0;
}

/* Mobile Menu */
.mobile-menu {
  position: fixed;
  top: 0;
  right: 0;
  width: var(--mobile-menu-width);
  height: 100vh;
  background: var(--mobile-menu-bg);
  transform: translateX(100%);
  transition: transform var(--transition-slow);
  z-index: 999;
  overflow-y: auto;
  box-shadow: -4px 0 24px rgba(0, 0, 0, 0.1);
  will-change: transform;
}

.mobile-menu.active {
  transform: translateX(0);
}

.mobile-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  opacity: 0;
  visibility: hidden;
  transition: all var(--transition-base);
  z-index: 998;
}

.mobile-overlay.active {
  opacity: 1;
  visibility: visible;
}

.mobile-menu-content {
  padding: 80px 24px 24px;
}

.mobile-menu-header {
  padding: 20px 24px;
  border-bottom: 1px solid var(--border-color);
  background: var(--bg-header);
  margin-bottom: 20px;
  border-radius: 12px;
  box-shadow: var(--shadow-sm);
}

.mobile-user-info {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 16px;
}

.mobile-user-avatar {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: var(--primary-color);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: 600;
  font-size: 18px;
}

.mobile-user-name {
  font-size: 18px;
  font-weight: 600;
  color: var(--text-color);
}

.mobile-user-email {
  font-size: 14px;
  color: rgba(var(--text-color), 0.7);
}

.mobile-menu-nav {
  margin-bottom: 32px;
}

.mobile-nav-item {
  display: block;
  padding: 16px 0;
  color: var(--text-color);
  font-size: 16px;
  font-weight: 500;
  text-decoration: none;
  border-bottom: 1px solid var(--border-color);
  transition: all var(--transition-fast);
}

.mobile-nav-item:hover {
  color: var(--primary-color);
  padding-left: 8px;
}

.mobile-menu-actions {
  padding-top: 20px;
  border-top: 1px solid var(--border-color);
}

.mobile-action-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 0;
}

/* Responsive Design */
@media (max-width: 1024px) {
  .header-container {
    grid-template-columns: 150px 1fr auto;
    gap: 20px;
    padding: 0 20px;
  }
  
  .nav-link {
    padding: 6px 12px;
    font-size: 14px;
  }
}

@media (max-width: 768px) {
  .enterprise-header {
    height: var(--header-mobile-height);
  }
  
  .header-container {
    grid-template-columns: 150px 1fr;
    padding: 0 16px;
  }
  
  .header-nav,
  .switcher-group,
  .theme-toggle {
    display: none;
  }
  
  .mobile-toggle {
    display: flex;
  }
  
  .header-actions {
    justify-content: flex-end;
  }
  
  .auth-group {
    display: none;
  }
}

/* Animations */
@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.header-loaded .nav-item {
  animation: slideIn var(--transition-base) ease-out backwards;
}

.header-loaded .nav-item:nth-child(1) { animation-delay: 50ms; }
.header-loaded .nav-item:nth-child(2) { animation-delay: 100ms; }
.header-loaded .nav-item:nth-child(3) { animation-delay: 150ms; }
.header-loaded .nav-item:nth-child(4) { animation-delay: 200ms; }
</style>

<header class="enterprise-header" id="enterprise-header">
  <div class="header-container">
    <!-- Logo -->
    <div class="header-logo">
      <a href="{{ routes.root_url }}">
        <img 
          src="https://cdn.shopify.com/s/files/1/0633/9012/2175/files/Simbye_Company_logo.webp?v=1739551616"
          alt="{{ shop.name }}"
          width="200"
          height="32"
          loading="eager"
          fetchpriority="high"
        >
      </a>
    </div>
    
    <!-- Desktop Navigation -->
    <nav class="header-nav" role="navigation">
      <div class="nav-item">
        <a href="/pages/how-it-works" class="nav-link">How eSIM Works</a>
      </div>
      <div class="nav-item">
        <a href="/pages/check-esim" class="nav-link">Check eSIM</a>
      </div>
      <div class="nav-item">
        <a href="/pages/faq" class="nav-link">FAQ</a>
      </div>
      <div class="nav-item">
        <a href="/pages/support" class="nav-link">Support</a>
      </div>
    </nav>
    
    <!-- Actions -->
    <div class="header-actions">
      <!-- Auth Section -->
      {% if customer %}
        <a href="{{ routes.account_url }}" class="auth-btn logged-in">
          <svg width="16" height="16" viewBox="0 0 20 20" fill="none">
            <circle cx="10" cy="7" r="3" stroke="currentColor" stroke-width="1.5"/>
            <path d="M4 18c0-3.3 2.7-6 6-6s6 2.7 6 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          </svg>
          {% if customer.first_name %}
            {{ customer.first_name }}
          {% else %}
            My Account
          {% endif %}
        </a>
      {% else %}
        <div class="auth-group">
          <a href="{{ routes.account_login_url }}" class="auth-btn login">Login</a>
          <a href="{{ routes.account_register_url }}" class="auth-btn register">Sign Up</a>
        </div>
      {% endif %}
      
      <!-- Language & Currency -->
      <div class="switcher-group">
        <button class="switcher" id="language-switcher" aria-label="Change language">
          <span class="switcher-icon">🌍</span>
          <span>EN</span>
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
            <path d="M3 4.5L6 7.5L9 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          </svg>
        </button>
        
        <button class="switcher" id="currency-switcher" aria-label="Change currency">
          <span>€ EUR</span>
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
            <path d="M3 4.5L6 7.5L9 4.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          </svg>
        </button>
      </div>
      
      <!-- Dark Mode Toggle -->
      <button class="theme-toggle" id="theme-toggle" aria-label="Toggle dark mode">
        <svg class="sun-icon" width="20" height="20" viewBox="0 0 20 20" fill="none" style="display: none;">
          <circle cx="10" cy="10" r="4" stroke="currentColor" stroke-width="1.5"/>
          <path d="M10 1v2M10 17v2M19 10h-2M3 10H1M16.4 3.6l-1.4 1.4M5 15l-1.4 1.4M16.4 16.4l-1.4-1.4M5 5L3.6 3.6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
        <svg class="moon-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M8 2C8 7.5 12.5 12 18 12c-1 4-5 6-9 6-5 0-9-4-9-9 0-4 2-8 6-9z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </button>
      
      <!-- Mobile Menu Toggle -->
      <button class="mobile-toggle" id="mobile-toggle" aria-label="Toggle menu">
        <div class="menu-icon">
          <span></span>
          <span></span>
          <span></span>
          <span></span>
          <span></span>
          <span></span>
          <span></span>
          <span></span>
          <span></span>
        </div>
      </button>
    </div>
  </div>
</header>

<!-- Mobile Menu -->
<div class="mobile-overlay" id="mobile-overlay"></div>
<nav class="mobile-menu" id="mobile-menu" role="navigation">
  <div class="mobile-menu-content">
    <!-- User Info -->
    {% if customer %}
      <div class="mobile-menu-header">
        <div class="mobile-user-info">
          <div class="mobile-user-avatar">
            {% if customer.first_name %}
              {{ customer.first_name | slice: 0 }}{{ customer.last_name | slice: 0 }}
            {% else %}
              SU
            {% endif %}
          </div>
          <div>
            <div class="mobile-user-name">
              {% if customer.first_name or customer.last_name %}
                {{ customer.first_name }} {{ customer.last_name }}
              {% elsif customer.name %}
                {{ customer.name }}
              {% else %}
                Simbye User
              {% endif %}
            </div>
            <div class="mobile-user-email">{{ customer.email }}</div>
          </div>
        </div>
        <a href="{{ routes.account_url }}" class="auth-btn logged-in" style="width: 100%; justify-content: center;">
          My Account →
        </a>
      </div>
    {% else %}
      <div class="mobile-menu-header">
        <div style="text-align: center; margin-bottom: 16px;">
          <p style="color: var(--text-color); font-size: 14px; opacity: 0.7;">Welcome to Simbye</p>
        </div>
        <div class="auth-group" style="width: 100%; height: 44px;">
          <a href="{{ routes.account_login_url }}" class="auth-btn login" style="flex: 1; height: 100%;">Login</a>
          <a href="{{ routes.account_register_url }}" class="auth-btn register" style="flex: 1; height: 100%;">Sign Up</a>
        </div>
      </div>
    {% endif %}
    
    <!-- Navigation -->
    <nav class="mobile-menu-nav">
      <a href="/pages/how-it-works" class="mobile-nav-item">How eSIM Works</a>
      <a href="/pages/check-esim" class="mobile-nav-item">Check eSIM</a>
      <a href="/pages/faq" class="mobile-nav-item">FAQ</a>
      <a href="/pages/support" class="mobile-nav-item">Support</a>
    </nav>
    
    <!-- Actions -->
    <div class="mobile-menu-actions">
      <div class="mobile-action-row">
        <span>Language</span>
        <button class="switcher" style="border: none;">
          <span class="switcher-icon">🌍</span>
          <span>English</span>
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
            <path d="M7.5 4.5L4.5 7.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          </svg>
        </button>
      </div>
      
      <div class="mobile-action-row">
        <span>Currency</span>
        <button class="switcher" style="border: none;">
          <span>€ EUR</span>
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
            <path d="M7.5 4.5L4.5 7.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          </svg>
        </button>
      </div>
      
      <div class="mobile-action-row">
        <span>Dark Mode</span>
        <button class="theme-toggle" style="border: none;">
          <svg class="moon-icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
            <path d="M8 2C8 7.5 12.5 12 18 12c-1 4-5 6-9 6-5 0-9-4-9-9 0-4 2-8 6-9z" stroke="currentColor" stroke-width="1.5"/>
          </svg>
        </button>
      </div>
    </div>
    
    <!-- Footer -->
    <div style="margin-top: auto; padding: 20px 0; opacity: 0.5; font-size: 12px;">
      v2.1.0 | © 2025 Simbye
    </div>
  </div>
</nav>

<script>
// Enterprise Header JavaScript - Vanilla JS, No jQuery
(function() {
  'use strict';
  
  // Elements
  const header = document.getElementById('enterprise-header');
  const mobileToggle = document.getElementById('mobile-toggle');
  const mobileMenu = document.getElementById('mobile-menu');
  const mobileOverlay = document.getElementById('mobile-overlay');
  const themeToggles = document.querySelectorAll('#theme-toggle, .mobile-menu-actions .theme-toggle');
  const languageSwitcher = document.getElementById('language-switcher');
  const currencySwitcher = document.getElementById('currency-switcher');
  
  // State
  let isScrolled = false;
  let isMobileMenuOpen = false;
  let currentTheme = localStorage.getItem('theme') || 'light';
  
  // Initialize
  function init() {
    // Set initial theme
    document.documentElement.setAttribute('data-theme', currentTheme);
    updateThemeIcons();
    
    // Add loaded class for animations
    setTimeout(() => {
      header.classList.add('header-loaded');
    }, 100);
    
    // Bind events
    bindEvents();
    
    // Check initial scroll position
    handleScroll();
  }
  
  // Event Bindings
  function bindEvents() {
    // Scroll event with throttle
    let scrollTimeout;
    window.addEventListener('scroll', () => {
      if (scrollTimeout) return;
      scrollTimeout = setTimeout(() => {
        scrollTimeout = null;
        handleScroll();
      }, 100);
    }, { passive: true });
    
    // Mobile menu
    if (mobileToggle) {
      mobileToggle.addEventListener('click', toggleMobileMenu);
    }
    
    if (mobileOverlay) {
      mobileOverlay.addEventListener('click', closeMobileMenu);
    }
    
    // Theme toggle
    themeToggles.forEach(toggle => {
      toggle.addEventListener('click', toggleTheme);
    });
    
    // Escape key to close mobile menu
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && isMobileMenuOpen) {
        closeMobileMenu();
      }
    });
    
    // Swipe to close mobile menu
    let touchStartX = 0;
    mobileMenu.addEventListener('touchstart', (e) => {
      touchStartX = e.touches[0].clientX;
    }, { passive: true });
    
    mobileMenu.addEventListener('touchend', (e) => {
      const touchEndX = e.changedTouches[0].clientX;
      if (touchEndX - touchStartX > 100) {
        closeMobileMenu();
      }
    }, { passive: true });
  }
  
  // Scroll Handler
  function handleScroll() {
    const scrollY = window.scrollY;
    if (scrollY > 10 && !isScrolled) {
      isScrolled = true;
      header.classList.add('scrolled');
    } else if (scrollY <= 10 && isScrolled) {
      isScrolled = false;
      header.classList.remove('scrolled');
    }
  }
  
  // Mobile Menu Functions
  function toggleMobileMenu() {
    isMobileMenuOpen ? closeMobileMenu() : openMobileMenu();
  }
  
  function openMobileMenu() {
    isMobileMenuOpen = true;
    mobileToggle.classList.add('active');
    mobileMenu.classList.add('active');
    mobileOverlay.classList.add('active');
    document.body.style.overflow = 'hidden';
    
    // Trap focus in mobile menu
    mobileMenu.setAttribute('aria-hidden', 'false');
    mobileMenu.focus();
  }
  
  function closeMobileMenu() {
    isMobileMenuOpen = false;
    mobileToggle.classList.remove('active');
    mobileMenu.classList.remove('active');
    mobileOverlay.classList.remove('active');
    document.body.style.overflow = '';
    
    mobileMenu.setAttribute('aria-hidden', 'true');
  }
  
  // Theme Toggle
  function toggleTheme() {
    currentTheme = currentTheme === 'light' ? 'dark' : 'light';
    document.documentElement.setAttribute('data-theme', currentTheme);
    localStorage.setItem('theme', currentTheme);
    updateThemeIcons();
  }
  
  function updateThemeIcons() {
    document.querySelectorAll('.sun-icon').forEach(icon => {
      icon.style.display = currentTheme === 'dark' ? 'block' : 'none';
    });
    document.querySelectorAll('.moon-icon').forEach(icon => {
      icon.style.display = currentTheme === 'light' ? 'block' : 'none';
    });
  }
  
  // Mark active navigation item
  function markActiveNavItem() {
    const currentPath = window.location.pathname;
    document.querySelectorAll('.nav-link, .mobile-nav-item').forEach(link => {
      if (link.getAttribute('href') === currentPath) {
        link.classList.add('active');
      }
    });
  }
  
  // Check for reduced motion preference
  function prefersReducedMotion() {
    return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  }
  
  // Disable animations if user prefers reduced motion
  if (prefersReducedMotion()) {
    document.documentElement.style.setProperty('--transition-fast', '0ms');
    document.documentElement.style.setProperty('--transition-base', '0ms');
    document.documentElement.style.setProperty('--transition-slow', '0ms');
  }
  
  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
  
  // Export for use in other scripts if needed
  window.EnterpriseHeader = {
    openMobileMenu,
    closeMobileMenu,
    toggleTheme
  };
})();
</script>

<!-- Spacer to prevent content from hiding under fixed header -->
<div style="height: var(--header-height); pointer-events: none;"></div>
<div style="height: var(--header-mobile-height); pointer-events: none; display: none;"></div>

<style>
@media (max-width: 768px) {
  div[style*="--header-height"] {
    display: none !important;
  }
  div[style*="--header-mobile-height"] {
    display: block !important;
  }
}
</style>