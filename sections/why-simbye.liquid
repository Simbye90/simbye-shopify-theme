<!-- sections/why-simbye.liquid -->
<!-- WHY SIMBYE - ENTERPRISE FEATURES SECTION -->

<style>
/* CSS Variables - Enterprise Design System */
:root {
  --why-bg: #ffffff;
  --why-text: #1a1a1a;
  --why-text-secondary: #666666;
  --why-primary: #009a61;
  --why-primary-hover: #00824f;
  --why-accent: #00d97e;
  --why-card-bg: #ffffff;
  --why-border: rgba(0, 0, 0, 0.08);
  --why-shadow-sm: 0 4px 12px rgba(0, 0, 0, 0.06);
  --why-shadow-md: 0 8px 24px rgba(0, 0, 0, 0.08);
  --why-shadow-lg: 0 12px 32px rgba(0, 154, 97, 0.15);
  --why-gradient: linear-gradient(135deg, #f8fffe 0%, #ffffff 100%);
  --why-transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Dark Mode Variables */
[data-theme="dark"] {
  --why-bg: linear-gradient(180deg, #0a0a0a 0%, #0f1210 50%, #0a0a0a 100%);
  --why-text: #f5f5f5;
  --why-text-secondary: #999999;
  --why-primary: #00d97e;
  --why-primary-hover: #009a61;
  --why-accent: #00ff88;
  --why-card-bg: rgba(26, 26, 26, 0.8);
  --why-border: rgba(255, 255, 255, 0.08);
  --why-shadow-sm: 0 4px 12px rgba(0, 0, 0, 0.3);
  --why-shadow-md: 0 8px 24px rgba(0, 0, 0, 0.4);
  --why-shadow-lg: 0 12px 32px rgba(0, 217, 126, 0.15);
  --why-gradient: linear-gradient(135deg, rgba(0, 217, 126, 0.05) 0%, transparent 100%);
}

/* Main Section */
.why-section {
  width: 100%;
  padding: 100px 0;
  background: var(--why-bg);
  position: relative;
  overflow: hidden;
  transition: var(--why-transition);
}

/* Background Pattern */
.why-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-image: 
    radial-gradient(circle at 10% 90%, rgba(0, 154, 97, 0.03) 0%, transparent 40%),
    radial-gradient(circle at 90% 10%, rgba(0, 217, 126, 0.03) 0%, transparent 40%);
  pointer-events: none;
  z-index: 0;
}

/* Container */
.why-container {
  max-width: 1440px;
  margin: 0 auto;
  padding: 0 40px;
  position: relative;
  z-index: 1;
}

/* Header */
.why-header {
  text-align: center;
  margin-bottom: 72px;
  animation: fadeInUp 0.8s ease;
}

.why-title {
  font-size: clamp(36px, 5vw, 56px);
  font-weight: 700;
  color: var(--why-text);
  margin: 0 0 20px 0;
  letter-spacing: -0.02em;
  line-height: 1.1;
}

.why-title-highlight {
  background: var(--why-primary);
  color: white;
  padding: 4px 20px;
  border-radius: 12px;
  display: inline-block;
  margin: 0 8px;
}

[data-theme="dark"] .why-title-highlight {
  background: var(--why-primary);
  color: #0a0a0a;
}

.why-subtitle {
  font-size: 20px;
  color: var(--why-text-secondary);
  margin: 0;
  font-weight: 400;
  line-height: 1.5;
  max-width: 800px;
  margin: 0 auto;
}

/* Features Grid */
.why-features {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
  margin-bottom: 60px;
}

/* Feature Card */
.why-feature {
  background: var(--why-card-bg);
  border-radius: 24px;
  padding: 36px 28px;
  box-shadow: var(--why-shadow-md);
  transition: var(--why-transition);
  position: relative;
  overflow: hidden;
  border: 2px solid var(--why-border);
  animation: fadeInUp 0.9s ease both;
  animation-delay: calc(var(--feature-index) * 0.1s);
}

.why-feature:nth-child(1) { --feature-index: 1; }
.why-feature:nth-child(2) { --feature-index: 2; }
.why-feature:nth-child(3) { --feature-index: 3; }
.why-feature:nth-child(4) { --feature-index: 4; }
.why-feature:nth-child(5) { --feature-index: 5; }
.why-feature:nth-child(6) { --feature-index: 6; }

[data-theme="dark"] .why-feature {
  background: var(--why-card-bg);
  border-color: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(10px);
}

.why-feature:hover {
  transform: translateY(-8px);
  box-shadow: var(--why-shadow-lg);
  border-color: var(--why-primary);
}

[data-theme="dark"] .why-feature:hover {
  border-color: var(--why-primary);
  background: rgba(26, 26, 26, 0.9);
}

/* Feature Gradient Effect */
.why-feature::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(0, 154, 97, 0.08), 
    transparent);
  transition: left 0.8s ease;
}

.why-feature:hover::before {
  left: 100%;
}

/* Icon Container */
.why-icon {
  width: 64px;
  height: 64px;
  background: var(--why-gradient);
  border: 2px solid var(--why-primary);
  border-radius: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 24px;
  transition: var(--why-transition);
}

[data-theme="dark"] .why-icon {
  background: rgba(0, 217, 126, 0.1);
  border-color: var(--why-primary);
}

.why-feature:hover .why-icon {
  transform: rotate(5deg) scale(1.1);
  background: var(--why-primary);
}

.why-feature:hover .why-icon svg {
  color: white;
}

.why-icon svg {
  width: 32px;
  height: 32px;
  color: var(--why-primary);
  transition: var(--why-transition);
}

[data-theme="dark"] .why-icon svg {
  color: var(--why-primary);
}

/* Feature Content */
.why-feature-title {
  font-size: 24px;
  font-weight: 700;
  color: var(--why-text);
  margin: 0 0 16px 0;
  letter-spacing: -0.01em;
  line-height: 1.2;
}

.why-feature-subtitle {
  font-size: 17px;
  font-weight: 600;
  color: var(--why-primary);
  margin: 0 0 12px 0;
}

.why-feature-text {
  font-size: 15px;
  color: var(--why-text-secondary);
  line-height: 1.6;
  margin: 0;
}

/* Responsive - Tablet */
@media (max-width: 1024px) {
  .why-container {
    padding: 0 24px;
  }
  
  .why-features {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
  
  .why-feature {
    padding: 32px 24px;
  }
  
  .why-feature-title {
    font-size: 22px;
  }
  
  .why-feature-subtitle {
    font-size: 16px;
  }
}

/* Mobile */
@media (max-width: 768px) {
  .why-section {
    padding: 72px 0;
  }
  
  .why-container {
    padding: 0 16px;
  }
  
  .why-header {
    margin-bottom: 48px;
  }
  
  .why-title {
    font-size: 32px;
  }
  
  .why-subtitle {
    font-size: 17px;
    padding: 0 16px;
  }
  
  .why-features {
    grid-template-columns: 1fr;
    gap: 20px;
  }
  
  .why-feature {
    padding: 28px 20px;
    border-radius: 20px;
  }
  
  .why-icon {
    width: 56px;
    height: 56px;
    border-radius: 16px;
    margin-bottom: 20px;
  }
  
  .why-icon svg {
    width: 28px;
    height: 28px;
  }
  
  .why-feature-title {
    font-size: 20px;
    margin-bottom: 12px;
  }
  
  .why-feature-subtitle {
    font-size: 15px;
    margin-bottom: 10px;
  }
  
  .why-feature-text {
    font-size: 14px;
    line-height: 1.5;
  }
}

/* Very Small Screens */
@media (max-width: 480px) {
  .why-container {
    padding: 0 12px;
  }
  
  .why-title {
    font-size: 28px;
  }
  
  .why-subtitle {
    font-size: 16px;
    padding: 0 8px;
  }
  
  .why-feature {
    padding: 24px 16px;
  }
  
  .why-feature-title {
    font-size: 19px;
  }
  
  .why-feature-subtitle {
    font-size: 14px;
  }
  
  .why-feature-text {
    font-size: 13px;
  }
}

/* Animations */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Accessibility */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
</style>

<section class="why-section" id="why-section">
  <div class="why-container">
    
    <!-- Header -->
    <div class="why-header">
      <h2 class="why-title">
        {{ 'why.title_part1' | t | default: 'Warum' }} <span class="why-title-highlight">{{ 'why.title_part2' | t | default: 'Simbye eSIM' }}</span> {{ 'why.title_part3' | t | default: 'wählen' }}
      </h2>
      <p class="why-subtitle">
        {{ 'why.subtitle' | t | default: 'Die smarteste Wahl für mobiles Internet im Ausland - ohne versteckte Kosten' }}
      </p>
    </div>
    
    <!-- Features Grid -->
    <div class="why-features">
      
      <!-- Feature 1: Aufladbar -->
      <div class="why-feature">
        <div class="why-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M4 12a8 8 0 0 1 8-8V2.5M12 4a8 8 0 0 1 0 16v1.5M12 20a8 8 0 0 1-8-8h1.5"/>
            <circle cx="12" cy="12" r="1"/>
            <path d="M17 7l-5 5 5 5"/>
          </svg>
        </div>
        <h3 class="why-feature-title">{{ 'why.feature1_title' | t | default: 'Aufladbar & Flexibel' }}</h3>
        <p class="why-feature-subtitle">{{ 'why.feature1_subtitle' | t | default: 'Einmal kaufen - immer wieder aufladen' }}</p>
        <p class="why-feature-text">
          {{ 'why.feature1_text' | t | default: 'Keine Vertragsbindung, keine versteckten Kosten, keine Mindestlaufzeit. Laden Sie Ihre eSIM beliebig oft auf. Die flexibelste eSIM-Lösung am Markt.' }}
        </p>
      </div>
      
      <!-- Feature 2: Günstigster Anbieter -->
      <div class="why-feature">
        <div class="why-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
          </svg>
        </div>
        <h3 class="why-feature-title">{{ 'why.feature2_title' | t | default: 'Der Günstigste Anbieter' }}</h3>
        <p class="why-feature-subtitle">{{ 'why.feature2_subtitle' | t | default: 'Günstiger als die Konkurrenz' }}</p>
        <p class="why-feature-text">
          {{ 'why.feature2_text' | t | default: 'Wir sind nicht nur günstig - wir sind DIE günstigsten. Vergleichen Sie uns mit Airalo, Holafly oder Nomad - niemand unterbietet unsere Preise.' }}
        </p>
      </div>
      
      <!-- Feature 3: 24/7 Support -->
      <div class="why-feature">
        <div class="why-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>
          </svg>
        </div>
        <h3 class="why-feature-title">{{ 'why.feature3_title' | t | default: '24/7 Premium Support' }}</h3>
        <p class="why-feature-subtitle">{{ 'why.feature3_subtitle' | t | default: 'Schnellster Support - unter 60 Sekunden' }}</p>
        <p class="why-feature-text">
          {{ 'why.feature3_text' | t | default: 'Während andere Sie warten lassen, antworten wir in Sekunden. WhatsApp, Telegram, Live-Chat - 15 Support-Mitarbeiter nur für Sie. In 12 Sprachen.' }}
        </p>
      </div>
      
      <!-- Feature 4: Gratis Testen -->
      <div class="why-feature">
        <div class="why-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20 12 20 22 4 22 4 12"/>
            <rect x="2" y="7" width="20" height="5"/>
            <line x1="12" y1="22" x2="12" y2="7"/>
            <path d="M12 7H7.5a2.5 2.5 0 0 1 0-5C11 2 12 7 12 7z"/>
            <path d="M12 7h4.5a2.5 2.5 0 0 0 0-5C13 2 12 7 12 7z"/>
          </svg>
        </div>
        <h3 class="why-feature-title">{{ 'why.feature4_title' | t | default: 'Gratis Testen' }}</h3>
        <p class="why-feature-subtitle">{{ 'why.feature4_subtitle' | t | default: '100 MB völlig kostenlos' }}</p>
        <p class="why-feature-text">
          {{ 'why.feature4_text' | t | default: 'Testen Sie komplett gratis. Keine Kreditkarte, kein Trick. Wir sind so überzeugt von unserer Qualität, dass wir Ihnen 100 MB schenken.' }}
        </p>
      </div>
      
      <!-- Feature 5: Premium Netze -->
      <div class="why-feature">
        <div class="why-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12.55a11 11 0 0 1 14.08 0M8.53 9.03a5.5 5.5 0 0 1 6.95 0M12 6v1"/>
            <circle cx="12" cy="17" r="1"/>
            <path d="M2 16.1A5 5 0 0 1 5.9 20M22 16.1A5 5 0 0 0 18.1 20"/>
          </svg>
        </div>
        <h3 class="why-feature-title">{{ 'why.feature5_title' | t | default: 'Lokale Premium-Netze' }}</h3>
        <p class="why-feature-subtitle">{{ 'why.feature5_subtitle' | t | default: 'Schnellstes 5G - Unlimited verfügbar' }}</p>
        <p class="why-feature-text">
          {{ 'why.feature5_text' | t | default: 'Echte lokale Geschwindigkeiten bis 1 Gbit/s. Von 500 MB bis echtes Unlimited ohne Fair-Use-Policy. Premium-Roaming-Partner in 190+ Ländern.' }}
        </p>
      </div>
      
      <!-- Feature 6: 100K+ Kunden -->
      <div class="why-feature">
        <div class="why-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
            <circle cx="9" cy="7" r="4"/>
            <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
          </svg>
        </div>
        <h3 class="why-feature-title">{{ 'why.feature6_title' | t | default: 'Vertrauenswürdig' }}</h3>
        <p class="why-feature-subtitle">{{ 'why.feature6_subtitle' | t | default: 'Über 100.000+ zufriedene Kunden' }}</p>
        <p class="why-feature-text">
          {{ 'why.feature6_text' | t | default: '100.000+ Kunden weltweit vertrauen uns bereits. 4.9 Sterne Bewertung. Die erste Wahl für Geschäftsreisende und Digital Nomads.' }}
        </p>
      </div>
      
    </div>
    
  </div>
</section>

<script>
(function() {
  'use strict';
  
  // Cache DOM elements
  const section = document.getElementById('why-section');
  const features = document.querySelectorAll('.why-feature');
  
  // Initialize
  function init() {
    if (!section) return;
    
    // Animate on scroll
    animateOnScroll();
  }
  
  // Animate elements on scroll
  function animateOnScroll() {
    if ('IntersectionObserver' in window) {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.style.opacity = '1';
            entry.target.style.transform = 'translateY(0)';
          }
        });
      }, {
        threshold: 0.1,
        rootMargin: '50px'
      });
      
      // Observe all features
      features.forEach(feature => {
        feature.style.opacity = '0';
        feature.style.transform = 'translateY(30px)';
        feature.style.transition = 'all 0.6s ease';
        observer.observe(feature);
      });
    }
  }
  
  // Initialize on DOM ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
</script>

{% schema %}
{
  "name": "Why Simbye Section",
  "class": "why-section",
  "settings": [],
  "presets": [
    {
      "name": "Why Simbye"
    }
  ]
}
{% endschema %}